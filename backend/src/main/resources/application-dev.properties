# ---- Perfil DEV: maquina local ----
# Los valores despues de ":" son los predeterminados para trabajar en local.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/aldia_db}

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.expiracion-ms=${JWT_EXPIRACION_MS:86400000}
cors.origenes-permitidos=${CORS_ORIGENES:http://localhost:4200}
